import type { Processor } from 'unified';
import type { Root } from 'mdast';
declare const remarkWrapper: {
    createProcessor(): Promise<Processor>;
    unified(): Promise<Processor>;
    remarkParse(): Promise<any>;
    parseMarkdown(content: string): Promise<Root>;
};
export = remarkWrapper;
