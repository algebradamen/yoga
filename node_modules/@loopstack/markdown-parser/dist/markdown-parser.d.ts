export interface SimpleJSONSchema {
    type: string;
    properties?: Record<string, any>;
    required?: string[];
    items?: any;
}
export declare class MarkdownParser {
    private parseList;
    private parseChildValue;
    private parseLiteralValue;
    private parseArrayItemType;
    private parseValue;
    private parseArrayType;
    private parseStringType;
    lowerCaseFirst(str: string): string;
    private reduceAstToObject;
    private getAst;
    /**
     * Converts an object to match a JSON schema, reformatting object properties
     * that are defined as arrays in the schema.
     */
    reformatToMatchSchema(obj: any, schema: SimpleJSONSchema): any;
    parseToObject<T>(content: string, schema: SimpleJSONSchema): Promise<T>;
    parse<T>(content: string, schema: SimpleJSONSchema): Promise<T>;
    validate(obj: any, schema: SimpleJSONSchema): void;
}
export default MarkdownParser;
